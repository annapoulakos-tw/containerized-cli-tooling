# Skill: Rust Style

## Definition

Write idiomatic, boring Rust.

## Rules

- Prefer clear types and ownership over clever lifetimes.
- Use `Result` for recoverable errors.
- Avoid `unwrap`/`expect` outside tests unless justified.
- Prefer standard library before dependencies.
- Follow existing crate/module conventions.
